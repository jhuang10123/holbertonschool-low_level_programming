#include "holberton.h"
#include <stdlib.h>
/**
 * argstostr - concatenates all arguments of program
 * @ac: argument count
 * @av: argument vector
 * Return: pointer or NULL
 */
char *argstostr(int ac, char **av)
{
	int i, j;
	char *newstr;

	if (ac == 0 || av == NULL)
	{
		return (NULL);
	}
	return (newstr);
}
