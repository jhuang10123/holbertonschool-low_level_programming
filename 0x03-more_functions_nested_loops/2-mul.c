#include "holberton.h"
#include <stdio.h>
/**
 * mul - multiplies 2 numbers
 * @a: first number
 * @b: second number to multiply
 * Return: result
 */
int mul(int a, int b)
{
	int result = a * b;
	return (result);
}
